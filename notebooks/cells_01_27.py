# Cell 1
from pathlib import Path
import sys
ROOT=Path("/kaggle/working/indus-valley-documentary-factory")
sys.path.insert(0,str(ROOT))
from src.pipeline import STEPS
print(STEPS[1]())

# Cell 2
from pathlib import Path
import sys
ROOT=Path("/kaggle/working/indus-valley-documentary-factory")
sys.path.insert(0,str(ROOT))
from src.pipeline import STEPS
print(STEPS[2]())

# Cell 3
from pathlib import Path
import sys
ROOT=Path("/kaggle/working/indus-valley-documentary-factory")
sys.path.insert(0,str(ROOT))
from src.pipeline import STEPS
print(STEPS[3]())

# Cell 4
from pathlib import Path
import sys
ROOT=Path("/kaggle/working/indus-valley-documentary-factory")
sys.path.insert(0,str(ROOT))
from src.pipeline import STEPS
print(STEPS[4]())

# Cell 5
from pathlib import Path
import sys
ROOT=Path("/kaggle/working/indus-valley-documentary-factory")
sys.path.insert(0,str(ROOT))
from src.pipeline import STEPS
print(STEPS[5]())

# Cell 6
from pathlib import Path
import sys
ROOT=Path("/kaggle/working/indus-valley-documentary-factory")
sys.path.insert(0,str(ROOT))
from src.pipeline import STEPS
print(STEPS[6]())

# Cell 7
from pathlib import Path
import sys
ROOT=Path("/kaggle/working/indus-valley-documentary-factory")
sys.path.insert(0,str(ROOT))
from src.pipeline import STEPS
print(STEPS[7]())

# Cell 8
from pathlib import Path
import sys
ROOT=Path("/kaggle/working/indus-valley-documentary-factory")
sys.path.insert(0,str(ROOT))
from src.pipeline import STEPS
print(STEPS[8]())

# Cell 9
from pathlib import Path
import sys
ROOT=Path("/kaggle/working/indus-valley-documentary-factory")
sys.path.insert(0,str(ROOT))
from src.pipeline import STEPS
print(STEPS[9]())

# Cell 10
from pathlib import Path
import sys
ROOT=Path("/kaggle/working/indus-valley-documentary-factory")
sys.path.insert(0,str(ROOT))
from src.pipeline import STEPS
print(STEPS[10]())

# Cell 11
from pathlib import Path
import sys
ROOT=Path("/kaggle/working/indus-valley-documentary-factory")
sys.path.insert(0,str(ROOT))
from src.pipeline import STEPS
print(STEPS[11]())

# Cell 12
from pathlib import Path
import sys
ROOT=Path("/kaggle/working/indus-valley-documentary-factory")
sys.path.insert(0,str(ROOT))
from src.pipeline import STEPS
print(STEPS[12]())

# Cell 13
from pathlib import Path
import sys
ROOT=Path("/kaggle/working/indus-valley-documentary-factory")
sys.path.insert(0,str(ROOT))
from src.pipeline import STEPS
print(STEPS[13]())

# Cell 14
from pathlib import Path
import sys
ROOT=Path("/kaggle/working/indus-valley-documentary-factory")
sys.path.insert(0,str(ROOT))
from src.pipeline import STEPS
print(STEPS[14]())

# Cell 15
from pathlib import Path
import sys
ROOT=Path("/kaggle/working/indus-valley-documentary-factory")
sys.path.insert(0,str(ROOT))
from src.pipeline import STEPS
print(STEPS[15]())

# Cell 16
from pathlib import Path
import sys
ROOT=Path("/kaggle/working/indus-valley-documentary-factory")
sys.path.insert(0,str(ROOT))
from src.pipeline import STEPS
print(STEPS[16]())

# Cell 17
from pathlib import Path
import sys
ROOT=Path("/kaggle/working/indus-valley-documentary-factory")
sys.path.insert(0,str(ROOT))
from src.pipeline import STEPS
print(STEPS[17]())

# Cell 18
from pathlib import Path
import sys
ROOT=Path("/kaggle/working/indus-valley-documentary-factory")
sys.path.insert(0,str(ROOT))
from src.pipeline import STEPS
print(STEPS[18]())

# Cell 19
from pathlib import Path
import sys
ROOT=Path("/kaggle/working/indus-valley-documentary-factory")
sys.path.insert(0,str(ROOT))
from src.pipeline import STEPS
print(STEPS[19]())

# Cell 20
from pathlib import Path
import sys
ROOT=Path("/kaggle/working/indus-valley-documentary-factory")
sys.path.insert(0,str(ROOT))
from src.pipeline import STEPS
print(STEPS[20]())

# Cell 21
from pathlib import Path
import sys
ROOT=Path("/kaggle/working/indus-valley-documentary-factory")
sys.path.insert(0,str(ROOT))
from src.pipeline import STEPS
print(STEPS[21]())

# Cell 22
from pathlib import Path
import sys
ROOT=Path("/kaggle/working/indus-valley-documentary-factory")
sys.path.insert(0,str(ROOT))
from src.pipeline import STEPS
print(STEPS[22]())

# Cell 23
from pathlib import Path
import sys
ROOT=Path("/kaggle/working/indus-valley-documentary-factory")
sys.path.insert(0,str(ROOT))
from src.pipeline import STEPS
print(STEPS[23]())

# Cell 24
from pathlib import Path
import sys
ROOT=Path("/kaggle/working/indus-valley-documentary-factory")
sys.path.insert(0,str(ROOT))
from src.pipeline import STEPS
print(STEPS[24]())

# Cell 25
from pathlib import Path
import sys
ROOT=Path("/kaggle/working/indus-valley-documentary-factory")
sys.path.insert(0,str(ROOT))
from src.pipeline import STEPS
print(STEPS[25]())

# Cell 26
from pathlib import Path
import sys
ROOT=Path("/kaggle/working/indus-valley-documentary-factory")
sys.path.insert(0,str(ROOT))
from src.pipeline import STEPS
print(STEPS[26]())

# Cell 27
from pathlib import Path
import sys
ROOT=Path("/kaggle/working/indus-valley-documentary-factory")
sys.path.insert(0,str(ROOT))
from src.pipeline import STEPS
print(STEPS[27]())
